import QtQuick 2.15
import QtQuick.Controls 2.15
import QtQuick.Window 2.15

Window {
    color: '#f2f2f2'
    visible: true

    TextField {
        placeholderText: 'Accepts .md or .html (from Typora exported) file'
    }
}